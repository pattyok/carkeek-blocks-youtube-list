import '@justinribeiro/lite-youtube';
import 'slick-carousel';


(function( $ ) {
    // Use $ as usual here, e.g.:
    $(".slider-carousel.wp-block-carkeek-blocks-youtube-embed").slick();
})( jQuery );
